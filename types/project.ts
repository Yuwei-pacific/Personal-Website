// 项目类型：来自 TypeGen 生成的查询结果（sanity/sanity.types.ts），
// schema 或查询变更后运行 `npm run typegen` 即可同步，无需手动维护。
import type { PROJECTS_QUERYResult } from "@/sanity/sanity.types";

export type Project = PROJECTS_QUERYResult[number];
