// Keep the About share image at the About route segment so its page-level
// Open Graph metadata cannot replace the locale-level image metadata.
export {
  alt,
  contentType,
  default,
  generateStaticParams,
  size,
} from "../opengraph-image";
